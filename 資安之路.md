---
title: '資安之路Day1:'

---

# 🔐 資安之路 Day 1

## 1. ROT13

**ROT13** 是一種簡單的 substitution cipher（替換式密碼），會將每個英文字母往後移 **13 位**。

例如：

```text id="j9cfwf"
A → N
B → O
N → A
```

因為英文字母有 26 個，所以 ROT13 執行兩次就會回到原文：

```text id="jtsdbp"
ROT13(ROT13(text)) = text
```

### CTF 小技巧

如果看到：

```text id="9wny5s"
synt{...}
```

很有可能可以嘗試 ROT13：

```text id="sjg7ek"
synt{...} → flag{...}
```

---

## 2. wget

`wget` 可以從指定的 URL 下載檔案。

```bash id="jllfj1"
wget <URL>
```

例如：

```bash id="35u2fp"
wget https://example.com/file.zip
```

預設會下載到目前所在的 directory。

查看說明：

```bash id="u3ydcv"
wget --help
```

> `<URL>` 中的 `< >` 代表「替換成實際內容」，不是真的要輸入 `< >`。

---

## 3. chmod +x

如果檔案沒有 executable（執行）權限：

```bash id="ebukbm"
chmod +x filename
```

其中：

```text id="z2l41a"
chmod = change mode
+x    = 加上 executable 權限
```

接著：

```bash id="7qorv8"
./filename
```

即可執行目前 directory 中的程式。

查看權限：

```bash id="ccyk7l"
ls -l filename
```

例如：

```text id="o4dzdx"
-rwxr-xr-x
```

其中：

```text id="y8krv1"
r = read
w = write
x = execute
```

---

## 4. Tab Completion

在 Terminal 輸入 filename 或 directory 前幾個字母後按：

```text id="2vxhmv"
Tab
```

Terminal 可以自動補完名稱。

例如：

```text id="3v6gr8"
super_long_challenge_filename
```

只需要先輸入：

```bash id="2pg2nr"
./super
```

然後按 `Tab`。

**Tab Completion = 省時間 + 降低打錯 filename/path 的機率。**

---

## 5. SSH

**SSH（Secure Shell）** 用來安全地登入並操作遠端 Server。

```bash id="e1n5sv"
ssh <username>@<server-ip>
```

例如：

```bash id="h3x3l2"
ssh gabe@192.168.1.10
```

登入成功後，就可以在遠端 Server 上執行：

```bash id="wvkxrz"
ls
cd
pwd
cat
./program
```

SSH 預設使用：

```text id="x1dwsa"
TCP Port 22
```

核心概念：

```text id="kxlgk3"
SSH = 遠端登入 + 加密連線 + 執行遠端指令
```

---

## 6. Netcat（nc）

**Netcat (`nc`)** 是 networking tool，可以透過 **TCP / UDP 建立連線並收發資料**。

常見用途：

- 測試 IP / Port 是否能連線
- 連接 CTF challenge server
- 建立簡單 TCP / UDP client
- Listen 某個 port
- 手動傳送 / 接收資料

連接：

```bash id="wb1t3p"
nc <server-ip> <port>
```

例如：

```bash id="vp2u4i"
nc example.com 5000
```

代表：

```text id="ys2m5v"
我的電腦 ──TCP──> example.com:5000
```

監聽 port：

```bash id="fpb7zd"
nc -l 5000
```

---

## 7. SSH vs Netcat

| | SSH | Netcat (`nc`) |
|---|---|---|
| 主要用途 | 遠端登入 Server | TCP/UDP 連線、收發資料 |
| 遠端 Terminal | ✅ | ❌ |
| 加密 | ✅ | ❌ 一般 nc 沒有 |
| Authentication | ✅ | ❌ |
| TCP | ✅ | ✅ |
| UDP | ❌ | ✅ |
| CTF 常見用途 | 登入遠端 Linux machine | 連接 Challenge IP + Port |

最簡單記法：

```text id="ch7i9s"
SSH → 我要「登入」Server
nc  → 我要「連接」某個 IP:Port
```

---

## 8. Terminal 基礎

### 查看 Help

```bash id="k41i6v"
command --help
```

有些程式也使用：

```bash id="uf7cgw"
command -h
```

如果看到：

```text id="1o2hw7"
Pass me -h to learn what I can do
```

就是：

```bash id="md2xb4"
./program -h
```

### ZIP 解壓縮

```bash id="5lyxvr"
unzip filename.zip
```

### tar.gz 解壓縮

`.tar.gz`：

```text id="mxtg4j"
tar 打包 + gzip 壓縮
```

解壓：

```bash id="vynb3l"
tar -xzf filename.tar.gz
```

其中：

```text id="pjynsr"
-x = extract
-z = gzip
-f = 指定 file
```

---

# 🧠 Day 1 Cheat Sheet

```bash id="h7g63i"
# Download
wget <URL>

# 查看檔案
ls

# 查看目前位置
pwd

# 加 executable 權限
chmod +x filename

# 執行
./filename

# Help
./filename -h

# ZIP
unzip filename.zip

# tar.gz
tar -xzf filename.tar.gz

# SSH
ssh username@server-ip

# Netcat
nc server-ip port

# Netcat Listen
nc -l port
```

---

# 🔐 資安之路 Day 2

## 1. strings

`strings` 可以從 **binary file（二進位檔案）** 中找出 human-readable strings。

```bash id="dqzpyf"
strings <filename>
```

例如：

```bash id="v5pdye"
strings program
```

可能找到：

```text id="ywb2ac"
Hello World
password
picoCTF{...}
```

### Static Analysis

如果題目要求：

> Find the flag without running the program.

不要直接：

```bash id="46s8p4"
./program
```

而是可以先：

```bash id="nsfy99"
strings program
```

這屬於簡單的 **Static Analysis（靜態分析）**：

```text id="7bntad"
Static Analysis
→ 不執行程式
→ 分析檔案本身
```

---

## 2. grep

`grep` 用來從文字中搜尋符合條件的內容。

```bash id="h7y76p"
grep "target" filename
```

例如：

```bash id="vtsz67"
grep "picoCTF" data.txt
```

常用參數：

```bash id="a40h0w"
# Ignore case
grep -i "flag" filename

# 顯示 line number
grep -n "flag" filename

# Recursive search
grep -r "flag" .
```

其中：

```text id="14fknp"
-i = ignore case
-n = line number
-r = recursive
.  = current directory
```

---

## 3. Pipe `|`

Pipe：

```text id="35bziz"
|
```

會把**左邊 command 的 output 傳給右邊 command，當成 input**。

例如：

```bash id="iwgvyn"
strings program | grep "picoCTF"
```

流程：

```text id="f7sazm"
Binary
   ↓
strings program
   ↓
Readable Strings
   ↓
   |
   ↓
grep "picoCTF"
   ↓
picoCTF{...}
```

因此：

```bash id="ezvhck"
strings program | grep "picoCTF"
```

就是：

> 先找出 binary 裡的 readable strings，再從結果中搜尋 `picoCTF`。

---

## 4. robots.txt

網站的：

```text id="ex2ivz"
robots.txt
```

用來告訴 **search engine crawler** 哪些 path 不希望被 crawl。

通常位於：

```text id="kkj14q"
https://example.com/robots.txt
```

例如：

```text id="vjnznc"
User-agent: *
Disallow: /secret/
```

意思：

```text id="txw29p"
User-agent: *
→ 所有 crawler

Disallow: /secret/
→ 告訴 crawler 不要 crawl /secret/
```

在 CTF 中看到：

```text id="fg5icf"
Disallow: /secret/
```

通常就值得檢查：

```text id="23jmxp"
https://example.com/secret/
```

### ⚠️ 重要

`robots.txt` **不是 Security Mechanism**。

它只是給 crawler 的規則，任何人都可以查看，因此不能用它來真正保護敏感資料。

---

## 5. Browser Inspect / DevTools

Web CTF 中可以使用 Browser Developer Tools：

```text id="advyqv"
F12
```

或：

```text id="jbs7bn"
Ctrl + Shift + I
```

常用：

```text id="spduj2"
Elements     → HTML / DOM
Sources      → JavaScript / CSS
Network      → HTTP Request / Response
Application  → Cookies / Local Storage
Console      → JavaScript / Errors
```

查看原始 HTML：

```text id="e3mvof"
Ctrl + U → View Page Source
```

> DevTools 主要看到的是送到 browser 的 client-side code/data，不代表可以直接看到 Server 上的 backend source code。

---

# 🧠 Day 2 Cheat Sheet

```bash id="9aifp3"
# Binary → readable strings
strings filename

# 搜尋文字
grep "target" filename

# Ignore case
grep -i "flag" filename

# 顯示行號
grep -n "flag" filename

# Recursive search
grep -r "flag" .

# Binary 找 flag
strings filename | grep "picoCTF"
```

Web：

```text id="nacxqa"
/robots.txt       → 查看 crawler 規則
F12               → Developer Tools
Ctrl + Shift + I  → Developer Tools
Ctrl + U          → View Page Source
```

---

## Day 2 核心觀念

```text id="wsnviw"
strings    → Binary 中找 readable text
grep       → 搜尋特定文字
|          → Output → Input
robots.txt → Crawler rules
F12        → Browser DevTools
Ctrl + U   → HTML Source
```

### ⭐ 今天最重要的 Command

```bash id="t0j6vq"
strings filename | grep "picoCTF"
```

以及 Web CTF 拿到網站時，可以開始養成：

```text id="b79mnz"
① 看網站
② Inspect / F12
③ View Page Source
④ 看 /robots.txt
⑤ 看 Sources / JavaScript
⑥ 看 Network Request / Response
```

的基本 Recon 習慣。